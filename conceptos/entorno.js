let code = process.env.CODE || 'Sin código';
let web = process.env.web || 'Sin web';

console.log(`Código: ${code}`);
console.log(`User: ${web}`);

/* Desde la terminar para asignar una variable de entorno */

/* CODE=ABC123 node -nombre del archivo a ejecutar- */
